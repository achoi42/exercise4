spring.application.name = gateway-application

# Local config service
spring.cloud.config.uri=http://localhost:8888

# Gateway application local server
server.port = 8790

# Disable security
security.basic.enabled = false
